"""Routes in resume app."""
# from django.urls import path

# from .views import SingleResumeView
# from .views import ResumeIndexView


# app_name = "resume"
# urlpatterns = [
#     path("", ResumeIndexView.as_view(), name="index"),
#     path("<slug:slug>/", SingleResumeView.as_view(), name="detail"),
# ]
